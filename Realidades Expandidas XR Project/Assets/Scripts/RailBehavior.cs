using UnityEngine;

public class RailBehavior : MonoBehaviour
{
    public enum WallAxis { x , y , z};

    public WallAxis wallAxis;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
